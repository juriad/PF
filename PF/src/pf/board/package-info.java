/**
 * Board package contains classes for loading, saving and manipulating all kinds of Boards. 
 * <p>
 * The main class is {@link pf.board.Board} joins {@link pf.board.Grid} and {@link pf.graph.Graph}.
 *
 */
package pf.board;